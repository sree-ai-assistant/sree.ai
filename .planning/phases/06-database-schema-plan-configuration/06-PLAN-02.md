# Plan 02: RLS Policies for New Tables

---
wave: 1
depends_on: [01]
files_modified:
  - supabase/migrations/20260512000002_rls_policies.sql
requirements: [DB-04]
autonomous: true
---

<objective>
Create Row Level Security policies for `anonymous_users` and `usage_tracking` tables ensuring users can only access their own data. Anonymous users access via anon_id header, authenticated users via auth.uid().
</objective>

<must_haves>
- RLS enabled on anonymous_users and usage_tracking
- Anonymous users can only read/write their own records via anon_id
- Authenticated users can only read/write their own usage_tracking records
- No cross-user data leakage possible
</must_haves>

## Tasks

### Task 1: Create RLS migration file

<read_first>
- supabase/migrations/20240416000000_initial_schema.sql
- supabase/migrations/20260512000001_subscription_rate_limiting.sql
</read_first>

<action>
Create `supabase/migrations/20260512000002_rls_policies.sql` with:

```sql
-- ============================================================
-- Migration: RLS Policies for Subscription & Rate Limiting
-- Phase 6: Database Schema & Plan Configuration
-- ============================================================

-- 1. ENABLE RLS ON NEW TABLES
ALTER TABLE public.anonymous_users ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.usage_tracking ENABLE ROW LEVEL SECURITY;

-- 2. ANONYMOUS USERS POLICIES
-- Anonymous users identify via anon_id passed in request header
-- Service role (backend) can perform all operations

CREATE POLICY "Service role full access to anonymous_users"
  ON public.anonymous_users
  FOR ALL
  USING (auth.role() = 'service_role')
  WITH CHECK (auth.role() = 'service_role');

CREATE POLICY "Anonymous users can read own record"
  ON public.anonymous_users
  FOR SELECT
  USING (
    anon_id = current_setting('request.headers', true)::json->>'x-anon-id'
  );

-- 3. USAGE TRACKING POLICIES
-- Authenticated users: access own records via auth.uid()
-- Anonymous users: access own records via anon_id header
-- Service role: full access for backend operations

CREATE POLICY "Service role full access to usage_tracking"
  ON public.usage_tracking
  FOR ALL
  USING (auth.role() = 'service_role')
  WITH CHECK (auth.role() = 'service_role');

CREATE POLICY "Authenticated users can read own usage"
  ON public.usage_tracking
  FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "Anonymous users can read own usage"
  ON public.usage_tracking
  FOR SELECT
  USING (
    anon_id = current_setting('request.headers', true)::json->>'x-anon-id'
    AND user_id IS NULL
  );
```
</action>

<acceptance_criteria>
- File `supabase/migrations/20260512000002_rls_policies.sql` exists
- File contains `ALTER TABLE public.anonymous_users ENABLE ROW LEVEL SECURITY`
- File contains `ALTER TABLE public.usage_tracking ENABLE ROW LEVEL SECURITY`
- File contains `Service role full access to anonymous_users`
- File contains `auth.uid() = user_id` for authenticated usage access
- File contains `x-anon-id` header reference for anonymous access
</acceptance_criteria>
